describe( 'Route component', function() {
  beforeEach( module( 'webComponentsPresentation.route' ) );

  it( 'should have a dummy test', inject( function() {
    expect( true ).toBeTruthy();
  }));
});
